Работа из командной строки:
Соединение:
  - -dbType DBTYPE -dbName DBNAME -host HOST -port PORT -user -USER -password PASSWORD
  - -connString jdbc:mysql://jaba.gib.loc:3306/gibloc -user USER -password PASSWORD

1. Новый клиент (newCli)
  - -cfg -in FILENAME
  - -nocfg

2. Новый файлсет (newFS)
  -
3. Проверка (check)
  - с конфигом (-cfg filename [-save])
  - из командной строки (-cliName XXXXXX [-save])
  - -out / -noout
